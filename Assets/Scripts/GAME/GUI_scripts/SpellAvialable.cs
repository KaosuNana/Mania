using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class SpellAvialable : MonoBehaviour {
    /*
    public Sprite spriteLock;
    public Sprite spriteNoLock;
    public void CheckSpellAvialable() {
        foreach (Transform ch in transform) {
            if (!HeroInformation.player.spellWarrior[ch.GetSiblingIndex()].CheckAvialable())
                ch.GetChild( 0 ).GetComponent<Image>().enabled = true;
            else {
                if (HeroInformation.player.spellWarrior[ch.GetSiblingIndex()].enable)
                    ch.GetChild( 0 ).GetComponent<Image>().enabled = false;
                else {
                    ch.GetChild( 0 ).GetComponent<Image>().enabled = true;
                    ch.GetChild( 0 ).GetComponent<Image>().sprite = spriteNoLock;
                }
            }
        }
    }
    */
}
